spring:
  application:
    name: AccountService

server:
  port: 8802

eureka:
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl.defaultZone: http://localhost:8701/eureka,http://localhost:8702/eureka
